{
  "name": "finance-app-tests",
  "version": "1.0.0",
  "description": "Tests E2E pour FinanceApp",
  "scripts": {
    "test": "npx playwright test --reporter=list",
    "test:headed": "npx playwright test --headed --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
